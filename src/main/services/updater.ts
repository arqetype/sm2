import { createLogger } from '../../lib/logger';

const logger = createLogger('AutoUpdater');

export function setupAutoUpdater() {
  logger.info('Auto-updater service initialized (skeleton only)');
}
