import { DialogContent } from '../ui/dialog';

export function CreateCardWithAiDialog() {
  return (
    <DialogContent showCloseButton={false}>
      <p>Hello from ai creation</p>
    </DialogContent>
  );
}
